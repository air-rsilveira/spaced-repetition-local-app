# contexts

React context providers and their related hooks.

- Providers are Client Components (`"use client"`).
- Name hooks in `camelCase` prefixed with `use` (e.g. `useDeck`).
- Import via the `@/contexts` path alias.
