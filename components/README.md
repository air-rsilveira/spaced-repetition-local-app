# components

Reusable UI components (presentational and composite).

- Name components in `PascalCase`.
- Add `"use client"` only when a component needs interactivity, state, effects, or browser APIs.
- Colocate component-specific helpers alongside the component.
- Import via the `@/components` path alias.
